package dev.jordond.connectivity.convention.plugin

import org.gradle.api.Plugin
import org.gradle.api.Project

class MultiplatformConventionPlugin : Plugin<Project> {

    override fun apply(target: Project) {}
}